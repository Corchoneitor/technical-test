export class VehicleService {
	constructor() {}

	/*
		Returns vehicle's type of arrival depending on how close to reservation's scheduled time it arrives.

	*/
	public getArriveTimeType(): any {
		// YOUR CODE HERE.
	}

	/*
		Returns if driver arrives on time to new reservation.
	*/
	public async canArriveAtTime(): Promise<any> {
		return new Promise(async (resolve, reject) => {
			// YOUR CODE HERE.
		});
	}
}
